# Progress Ledger

Append-only workflow record for the operator layer.

## Table of Contents

- [Format](#format)
- [Hyperdata Fields](#hyperdata-fields)
- [Entries](#entries)

## Format

Each entry uses a UTC ISO-8601 timestamp and records the mission, issue map, owner lane, Ralph loop, current ref, status, evidence, and next step.

## Hyperdata Fields

- `Mission`: the canonical mission file or the repo-operating-system slice.
- `Issues`: the linked GitHub issues for the slice.
- `Lane`: the owning operator lane.
- `Loop`: the Ralph loop or validation command used as the evidence path.
- `Ref`: the current branch or commit context when the entry was recorded.
- `Status`: `in_progress`, `validated`, or `landed`.

## Entries

| Timestamp (UTC) | Mission | Issues | Lane | Loop | Ref | Status | Evidence | Next Step |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-04-02T00:14:55Z | `openspec/changes/reach-daily-driver` | `#10`, `#12` | `core`, `qa`, `architect` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs --seed 0`; `TMPDIR=$PWD/.tmp mix escript.build`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `printf 'hi!\n/quit\n' | pikachu`; `printf '/kimi\n/quit\n' | pikachu` | `feat/roadmap-competitive-plan@working-tree` | `validated` | made the TUI feel more like an actual operator client by treating plain text as chat input by default while keeping slash-prefixed input for local controls; added direct one-token provider aliases like `/kimi`, `/glm`, `/nim`, and `/generic` on top of `/provider ...`, updated the TUI docs and OpenSpec contract, and proved both paths live through `pikachu`: `hi!` now completed a real chat instead of rendering an unknown-command error, and `/kimi` switched the active provider surface to Kimi with an explicit notice | the next UI step should be a real pane or composer upgrade, not a second runtime; the remaining readiness blockers are still live Kimi proof once credentials exist and the first explicit RC or non-RC decision record |
| 2026-04-02T00:02:11Z | `openspec/changes/reach-daily-driver` | `#10`, `#12` | `core`, `qa`, `architect` | `TMPDIR=$PWD/.tmp mix test test/claw_code/cli_test.exs test/claw_code/tui_test.exs --seed 0`; `TMPDIR=$PWD/.tmp mix format --check-formatted`; `TMPDIR=$PWD/.tmp mix test`; `TMPDIR=$PWD/.tmp mix escript.build`; `./claw_code summary`; `./claw_code symphony --native "review MCP tool"`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `./claw_code install`; `pikachu summary`; `printf '/provider kimi\nquit\n' | pikachu`; `bash -lc 'command -v pikachu && pikachu summary | sed -n "1,4p"'` | `feat/roadmap-competitive-plan@working-tree` | `validated` | renamed the stable launcher default from `claw` to `pikachu`, added focused CLI coverage so `install` now defaults to `pikachu` while keeping `--as ...` replacement and conflict behavior explicit, and added TUI slash-command aliases that route `/provider ...`, `/model ...`, `/base-url ...`, `/clear ...`, `/tools ...`, `/probe`, `/help`, and `/quit` through the same existing thin-client control path instead of inventing a second parser; updated the README, TUI quickstart, and OpenSpec spec/tasks/design so the current operator path is `pikachu` plus slash-style TUI controls, full repo validation passed with 202 tests green, the real local install is now live at `/Users/s3nik/.local/bin/pikachu`, and the smoke run through `pikachu` showed both `provider=kimi` and `notice=Provider set to kimi.` after `/provider kimi` inside the default TUI launch | the remaining daily-driver blockers are still live Kimi proof once credentials exist and the first explicit RC or non-RC decision record |
| 2026-04-01T23:46:17Z | `openspec/changes/reach-daily-driver` | `#10`, `#12`, `#13` | `core`, `qa`, `architect` | `TMPDIR=$PWD/.tmp mix format --check-formatted`; `TMPDIR=$PWD/.tmp mix test`; `TMPDIR=$PWD/.tmp mix escript.build`; `./claw_code summary`; `./claw_code symphony --native "review MCP tool"`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `./claw_code install --as claw`; `claw summary`; `printf 'quit\n' | claw`; `bash -lc 'command -v claw'`; `bash -lc 'claw summary'` | `feat/roadmap-competitive-plan@working-tree` | `validated` | added a first-class `install` command that writes a stable local launcher, defaults that launcher to the TUI on zero args, and still forwards explicit subcommands to the existing CLI; strengthened the path with focused CLI coverage for install, conflict, and `--force` replacement behavior; updated README and TUI/OpenSpec docs to make the one-command launcher an explicit operator path; full repo validation passed with 201 tests green; local install wrote `/Users/s3nik/.local/bin/claw`, `claw summary` passed, `printf 'quit\n' | claw` opened and exited the TUI cleanly, and `~/.bash_profile` now exports `~/.local/bin` so Bash login shells can resolve the launcher too | the remaining daily-driver blockers are still live Kimi proof once credentials exist and the first explicit RC or non-RC decision record |
| 2026-04-01T23:09:23Z | `openspec/changes/reach-daily-driver` | `#11`, `#10` | `providers`, `architect` | `TMPDIR=$PWD/.tmp mix test test/claw_code/provider_test.exs`; `set -a; source .env.local 2>/dev/null || true; set +a; ./claw_code doctor --provider kimi`; `set -a; source .env.local 2>/dev/null || true; set +a; ./claw_code providers | sed -n '/## kimi/,/## nim/p'`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `feat/roadmap-competitive-plan@working-tree` | `validated` | tightened provider-specific env resolution so `glm`, `nim`, and `kimi` no longer inherit shared `CLAW_*` connection vars, which fixes misleading cross-provider configuration like Kimi appearing to use a NIM-flavored shared API key; added a focused provider regression proving Kimi ignores shared `CLAW_API_KEY` or `CLAW_MODEL`, the direct Kimi `doctor` smoke now reports `api_key: missing`, the rebuilt provider matrix now lists only `KIMI_API_KEY | MOONSHOT_API_KEY` for Kimi, and full repo validation passed with 198 tests green | the remaining provider blocker is still live Kimi proof once credentials exist; after that, the next true readiness decision is the first explicit RC or non-RC entry in `progress.md` |
| 2026-04-01T22:53:08Z | `openspec/changes/reach-daily-driver` | `#11`, `#10`, `#12` | `providers`, `qa`, `architect` | `TMPDIR=$PWD/.tmp mix test test/claw_code/qa_script_test.exs`; `bash -n scripts/*.sh`; `set -a; source .env.local 2>/dev/null || true; set +a; TMPDIR=$PWD/.tmp CLAW_PROVIDER_LIVE=glm,nim,generic CLAW_GENERIC_LIVE_BASE_URL=https://open.bigmodel.cn/api/coding/paas/v4 CLAW_GENERIC_LIVE_API_KEY="$GLM_API_KEY" CLAW_GENERIC_LIVE_MODEL=GLM-4.7 ./scripts/qa.sh provider-live`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `feat/roadmap-competitive-plan@working-tree` | `validated` | added `./scripts/qa.sh provider-live` plus `scripts/ralph-provider-live.sh` as the strict live-success lane for current provider evidence, fixed the first iteration to stay compatible with the system Bash and to avoid logging raw generic API keys in Ralph logs, and used that lane to prove `glm`, `nim`, and a real generic OpenAI-compatible endpoint backed by the BigModel coding URL; focused QA-script coverage passed with 4 tests green, the live provider lane completed successfully, and full repo validation passed with 197 tests green | the remaining provider blocker is now only Kimi live proof once credentials exist, then the last Phase 6 step is recording the first explicit RC or non-RC decision with evidence links |
| 2026-04-01T22:33:29Z | `openspec/changes/reach-daily-driver` | `#10`, `#12`, `#13` | `qa`, `architect`, `providers` | `TMPDIR=$PWD/.tmp mix test test/claw_code/qa_script_test.exs`; `TMPDIR=$PWD/.tmp ./scripts/qa.sh release`; `bash -n scripts/*.sh` | `feat/roadmap-competitive-plan@working-tree` | `validated` | closed the main Phase 6 documentation gap by adding a canonical release-confidence reference with the first daily-driver beta bar, grouped pre-RC checklist, and live smoke matrix, then tightened the release Ralph lane so it now includes the provider-matrix gate before daemon and recovery checks; the focused QA-script regression passed with 3 tests green, the full release Ralph loop passed end to end with the new provider-matrix leg, and the environment still shows GLM and NIM live while Kimi and generic remain explicit pending-proof lanes | keep the remaining blockers narrow: record the first release-candidate decision when the evidence is sufficient, and close the external live-provider gaps for Kimi and one generic endpoint |
| 2026-04-01T22:28:34Z | `openspec/changes/reach-daily-driver` | `#7`, `#10`, `#12` | `native`, `qa`, `architect` | `TMPDIR=$PWD/.tmp mix test test/claw_code/router_test.exs test/claw_code/qa_script_test.exs`; `TMPDIR=$PWD/.tmp ./scripts/qa.sh native`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `bash -n scripts/*.sh` | `feat/roadmap-competitive-plan@working-tree` | `validated` | closed the remaining Phase 3 native-evidence gap by proving the native Ralph lane now exercises both `--native` and explicit `--no-native` routing and symphony flows, adding a router regression that guarantees the native helper is never consulted when disabled, and tightening the release/architecture docs with a sharper Elixir-first versus Zig rule; focused tests passed with 5 tests green, the native Ralph lane completed with both native-enabled and native-disabled smokes, and full repo validation passed with 195 tests green | take the next remaining blockers in order: Phase 6 release-confidence docs and smoke matrix, then the live provider evidence gaps that still depend on available credentials and endpoints |
| 2026-03-31T16:41:56Z | repo-operating-system | `#10` | `architect` | repo inspection | `main@01bf54a` | `in_progress` | duplicate missions and Ralph shims identified; README lacked a canonical operator entrypoint | collapse to four canonical missions, wire the QA dispatcher, and record the normalized surface |
| 2026-03-31T16:44:15Z | repo-operating-system | `#10` | `architect`, `qa` | `./scripts/qa.sh release` | `main@working-tree` | `validated` | `bash -n scripts/*.sh`; `./scripts/qa.sh validate`; `./scripts/qa.sh provider`; `RALPH_MAX_CYCLES=1 ./scripts/qa.sh release` | keep the operator layer canonical and add release automation plus Mission 01 evidence |
| 2026-03-31T16:49:44Z | `missions/01-runtime-core.md` | `#6`, `#12` | `core` | `./scripts/qa.sh core` | `main@working-tree` | `validated` | immutable requirements-ledger persistence is covered by `test/claw_code/session_store_test.exs`; `mix test`; `./scripts/qa.sh validate`; `load-session` reports retained requirements | land the Mission 01 slice with a semantic commit, then move to the provider loop |
| 2026-03-31T16:49:44Z | `missions/04-release-readiness.md` | `#9`, `#10` | `qa` | `RALPH_MAX_CYCLES=1 ./scripts/qa.sh release` | `main@working-tree` | `validated` | full Ralph release gate passed; `npm ci --ignore-scripts`; local release fallback validated `.releaserc.json` because GitHub auth was absent; CI and release workflows are now in repo | land the release-readiness slice with a semantic commit and use a token-backed dry run on CI or on a machine with GitHub auth |
| 2026-03-31T16:51:44Z | `missions/01-runtime-core.md` | `#6`, `#12` | `core` | `./scripts/qa.sh core` | `main@fb2f88f` | `landed` | committed as `feat(core): add elixir runtime foundation`; 20 tests green before commit; requirements ledger persisted in saved sessions and surfaced through `load-session` | land the operator and release automation slice, then start Mission 02 for real provider work |
| 2026-03-31T16:52:15Z | repo-operating-system | `#10` | `architect`, `qa` | `./scripts/qa.sh release` | `main@dd0c515` | `landed` | committed as `docs(omx): add canonical operator layer`; four canonical mission briefs, Ralph loops, quality gates, and the UTC progress ledger are now tracked | land semantic-release automation and README release guidance, then start Mission 02 |
| 2026-03-31T16:52:48Z | `missions/04-release-readiness.md` | `#9`, `#10` | `qa` | `RALPH_MAX_CYCLES=1 ./scripts/qa.sh release` | `main@6bb3e79` | `landed` | committed as `ci(release): add semantic-release automation`; `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `package.json`, `.releaserc.json`, and `CHANGELOG.md` now define the release lane | start Mission 02 and run a token-backed semantic-release dry run on CI or on a machine with GitHub auth |
| 2026-03-31T17:07:31Z | `missions/02-provider-loop.md` | `#11`, `#5` | `providers`, `adapters` | `./scripts/qa.sh provider` | `main@working-tree` | `validated` | provider defaults now normalize `glm` and `nim`; shell/runtime tool calls emit receipts with status, duration, cwd, and output; `mix test`; `./scripts/qa.sh validate`; `./scripts/qa.sh provider` all passed | land the Mission 02 slice with a semantic commit, then wire a live GLM or NIM smoke on a machine with credentials |
| 2026-03-31T17:09:57Z | `missions/02-provider-loop.md` | `#11`, `#5` | `providers`, `adapters` | `./scripts/qa.sh provider` | `main@8caa799` | `landed` | committed as `feat(provider): add provider defaults and tool receipts`; provider docs are now in `docs/providers.md`; tool receipts persist through provider tool calls and `load-session` | start a live GLM or NIM smoke with explicit env vars instead of reading local secret files |
| 2026-03-31T17:19:13Z | `missions/02-provider-loop.md` | `#11` | `providers` | `CLAW_PROVIDER=kimi ./scripts/qa.sh provider` | `main@working-tree` | `validated` | first-class `kimi` provider defaults resolve to `https://api.moonshot.ai/v1` and `kimi-k2.5`; `mix test`; `./scripts/qa.sh provider`; `CLAW_PROVIDER=kimi ./scripts/qa.sh provider` all passed on the missing-key path | land the Kimi slice with a semantic commit, then add a live Kimi smoke using explicit env vars |
| 2026-03-31T17:19:35Z | `missions/02-provider-loop.md` | `#11` | `providers` | `CLAW_PROVIDER=kimi ./scripts/qa.sh provider` | `main@2f1147a` | `landed` | committed as `feat(provider): add kimi provider defaults`; Kimi now resolves to `https://api.moonshot.ai/v1` with model `kimi-k2.5`, and the Ralph provider loop recognizes `KIMI_API_KEY` and `MOONSHOT_API_KEY` | run a live Kimi smoke with explicit env vars, then move on to more BEAM-side reliability and native hardening |
| 2026-03-31T17:21:23Z | `missions/03-native-and-adapters.md` | `#7` | `native` | `./scripts/qa.sh native` | `main@working-tree` | `validated` | `NativeRanker.safe_rank/3` now returns explicit errors instead of forcing rescue-based fallback; `mix test test/claw_code/native_ranker_test.exs test/claw_code/router_test.exs`; `./scripts/qa.sh native` both passed | land the native fallback hardening slice with a semantic commit |
| 2026-03-31T17:21:42Z | `missions/03-native-and-adapters.md` | `#7` | `native` | `./scripts/qa.sh native` | `main@153af05` | `landed` | committed as `fix(native): harden fallback path`; router fallback now uses explicit `safe_rank/3` errors instead of rescue-driven control flow, with tests for invalid native output and router fallback | continue BEAM-side reliability work around sessions, supervision, and adapter receipts |
| 2026-03-31T17:35:48Z | `missions/01-runtime-core.md` | `#6` | `core` | `./scripts/qa.sh core` | `main@working-tree` | `validated` | supervised session owners now keep one session id hot per process, session files preserve `created_at` and `updated_at`, `resume-session` reuses the same session file, and `./scripts/qa.sh core` plus a manual `chat -> resume-session -> load-session` smoke both passed | land the resumable-session slice with a semantic commit |
| 2026-03-31T17:38:12Z | `missions/01-runtime-core.md` | `#6` | `core` | `./scripts/qa.sh core` | `main@76e3d9c` | `landed` | committed as `feat(core): add resumable session supervision`; one supervised owner process now fronts each resumed session id, `load-session` exposes `created_at` and `updated_at`, and both the core Ralph gate and the manual `chat -> resume-session -> load-session` smoke passed | keep the session path stable while expanding BEAM-side reliability around receipts, cancellation, and operator visibility |
| 2026-03-31T17:50:30Z | `missions/01-runtime-core.md` | `#6`, `#12` | `core` | `./scripts/qa.sh core` | `main@working-tree` | `validated` | `./claw_code sessions --limit 3` now exposes recent session ids and stop reasons; `./claw_code load-session <id> --show-messages --show-receipts` renders persisted conversation and receipt state; `mix test`, `./scripts/qa.sh core`, and `./scripts/qa.sh release` all passed with 44 tests green | land the CLI/session observability slice with a semantic commit |
| 2026-03-31T17:50:30Z | `missions/02-provider-loop.md` | `#11`, `#5` | `providers`, `qa` | `./scripts/qa.sh provider` | `main@working-tree` | `validated` | `./claw_code doctor --provider kimi --api-key test-key` now resolves the exact request URL and config source; missing-provider chats exit non-zero and the provider Ralph loop validates that expected failure explicitly; saved sessions now persist `api_key_present` instead of a raw provider secret; `./scripts/qa.sh provider` and `./scripts/qa.sh release` both passed | land the provider/operator hardening slice with a semantic commit, then run live Kimi or custom GLM smoke with explicit env vars |
| 2026-03-31T17:51:02Z | `missions/01-runtime-core.md` | `#6`, `#12` | `core` | `./scripts/qa.sh core` | `main@aae9221` | `landed` | committed as `feat(cli): inspect sessions and harden provider setup`; the CLI now lists recent sessions, loads persisted messages and receipts on demand, and keeps session inspection on the happy path instead of crashing on missing ids | use the same operator surface to validate live Kimi or custom GLM runs with explicit env vars |
| 2026-03-31T17:51:02Z | `missions/02-provider-loop.md` | `#11`, `#5` | `providers`, `qa` | `./scripts/qa.sh provider` | `main@aae9221` | `landed` | committed as `feat(cli): inspect sessions and harden provider setup`; `doctor` now accepts provider flags, missing-provider chat exits non-zero while the Ralph provider loop treats that as an expected negative validation, provider requests have explicit timeouts, and saved sessions no longer persist raw API keys | run a live Kimi or custom GLM smoke with explicit env vars and continue toward single-flight cancellable session execution |
| 2026-03-31T18:08:59Z | `missions/01-runtime-core.md` | `#6`, `#12` | `core` | `./scripts/qa.sh core` | `main@working-tree` | `validated` | single-flight session ownership is now enforced through OTP; concurrent chats on one session id return `session_busy`, `cancel-session` and `Runtime.cancel/2` can stop a hung run, checkpointed tool receipts persist before the final provider reply, and the full `mix test`, `./scripts/qa.sh core`, and `./scripts/qa.sh release` gates passed with 51 tests green | land the single-flight session control slice with a semantic commit |
| 2026-03-31T18:09:34Z | `missions/01-runtime-core.md` | `#6`, `#12` | `core` | `./scripts/qa.sh core` | `main@d9b1ff2` | `landed` | committed as `feat(core): add single-flight session control`; one session id now owns at most one active run, the runtime can cancel a hung session without losing persisted state, tool receipts checkpoint before the final provider reply, and session timestamps remain stable across repeated writes | keep building toward task-owned cancellation semantics and richer run inspection, but the core concurrency hazard is now closed |
| 2026-03-31T18:14:19Z | `missions/01-runtime-core.md` | `#6`, `#12` | `core`, `qa` | `./scripts/qa.sh release` | `main@9239e7c` | `landed` | committed as `fix(cli): clarify same-runtime session cancellation`; the CLI and README now say `cancel-session` only affects runs inside the same long-lived runtime, and the cancel-session fixture paths use random temp roots so repeated Ralph gates do not bleed session state across test invocations | keep the control path honest until a persistent daemon/node exists, then revisit cross-process cancellation semantics |
| 2026-03-31T18:12:51Z | `missions/01-runtime-core.md` | `#6`, `#12` | `core`, `qa` | `./scripts/qa.sh validate` | `main@142d07c` | `landed` | committed as `fix(core): restore session control coverage`; the session-control tests now use isolated temp roots so they do not interfere with each other, and the README once again documents `cancel-session` as part of the operator surface | continue toward task-owned cancellation semantics and richer run inspection, with the current session-control slice fully closed |
| 2026-03-31T18:14:31Z | `missions/01-runtime-core.md` | `#6`, `#12` | `core`, `qa` | `./scripts/qa.sh validate` | `main@9239e7c` | `landed` | committed as `fix(cli): clarify same-runtime session cancellation`; the CLI, README, and tests now describe the real cancellation boundary, so the operator surface no longer implies cross-process control that does not exist yet | build the next slice around cancellation semantics that can survive beyond a single long-lived BEAM instance |
| 2026-03-31T18:46:39Z | `docs/proposals/persistent-control-plane.md` | `#6`, `#10` | `core`, `qa` | `./scripts/qa.sh release` | `main@c9d5f25` | `landed` | committed as `feat(core): add daemon control plane`; local daemon lifecycle plus daemon-backed `chat` and `cancel-session` now work across CLI invocations, `mix test` passed with 58 tests, `./scripts/qa.sh daemon` passed, and the full `./scripts/qa.sh release` gate passed including the new daemon lane | decide whether to open a dedicated issue for daemon hardening, then extend replay/inspection semantics and stale-daemon recovery coverage |
| 2026-03-31T18:54:27Z | `docs/reference/architecture.md` | `#6`, `#10` | `core`, `qa` | `./scripts/qa.sh release` | `main@ddd9280` | `landed` | committed as `test(core): cover daemon operator semantics` and `docs(architecture): add elixir zig reference`; daemon-backed resume, stale-status reporting, and idle-session cancellation are now covered, the repo has a canonical Elixir-first architecture reference, and the full `./scripts/qa.sh release` gate passed with 63 tests green | keep pushing daemon hardening toward richer replay/inspection commands and clearer live-provider operator flows |
| 2026-03-31T18:54:39Z | `docs/reference/architecture.md` | `#13`, `#6` | `core`, `qa` | `./scripts/qa.sh release` | `main@working-tree` | `validated` | added a canonical architecture reference for the Elixir-first runtime with a narrow Zig boundary; daemon hardening now covers stale-metadata replacement and daemon-backed `resume-session`; `mix test test/claw_code/daemon_test.exs test/claw_code/cli_test.exs`, `./scripts/qa.sh daemon`, and the full `./scripts/qa.sh release` gate all passed with 63 tests green | land the architecture/hardening slice with a semantic commit and keep the next work focused on richer daemon inspection and recovery signals |
| 2026-03-31T19:08:00Z | `missions/02-provider-loop.md` | `#11` | `providers`, `qa` | `./scripts/qa.sh release` | `main@working-tree` | `validated` | generic OpenAI-compatible inference now supports no-auth endpoints; provider aliases normalize `local`, `custom`, `moonshot`, `bigmodel`, and `nvidia`; provider diagnostics now treat the generic API key as optional; end-to-end runtime coverage proves a generic endpoint works without an `Authorization` header; `mix test test/claw_code/provider_test.exs test/claw_code/runtime_test.exs` and the full `./scripts/qa.sh release` gate both passed with 65 tests green | land the provider hardening slice with a semantic commit, then run live GLM/Kimi/custom inference smoke using explicit env vars |
| 2026-03-31T19:20:54Z | `missions/02-provider-loop.md` | `#11`, `#13` | `providers`, `core`, `qa` | `./scripts/qa.sh release` | `main@working-tree` | `validated` | local runtime commands now autoload `.env.local` and `.env` outside test mode, git ignores local env files, and tool exposure is gated by prompt intent instead of always advertising tools; a copied local `.env.local` configured NIM successfully, `./claw_code doctor --provider nim` resolved a live provider, `./claw_code chat --provider nim "say hello and report the configured provider in one short line"` completed in one turn with zero tool receipts, and the full `./scripts/qa.sh release` gate passed with 68 tests green | land the env-loading and tool-gating slice with a semantic commit, then decide whether to make provider-specific tool policies explicit instead of heuristic-only |
| 2026-03-31T19:31:10Z | `missions/02-provider-loop.md` | `#11`, `#13` | `providers`, `core`, `qa` | `./scripts/qa.sh release` | `main@73dfa56` | `landed` | committed as `feat(core): load local env files and gate tool exposure`; local `.env.local` and `.env` files now autoload outside test mode, those files are git-ignored, a copied local `.env.local` configured NIM successfully, the live NIM chat completed in one turn with zero tool receipts, and the full release gate passed with 68 tests green | make tool exposure explicitly operator-controlled instead of heuristic-only for weaker inference endpoints |
| 2026-03-31T19:30:55Z | `missions/02-provider-loop.md` | `#11`, `#13` | `providers`, `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/qa.sh daemon` | `main@1b1f412` | `landed` | committed as `feat(core): add explicit tool policy controls`; `--tools`, `--no-tools`, and `CLAW_TOOL_MODE` now control tool exposure, `doctor` reports the active tool policy, daemon-backed chats preserve the override, `mix test test/claw_code/runtime_test.exs test/claw_code/cli_test.exs` passed with 36 tests green, `TMPDIR=$PWD/.tmp ./scripts/qa.sh daemon` passed with 30 tests green, and `./claw_code doctor --provider generic --no-tools` reported `tool_policy: disabled` | build the thin JSON and TUI client boundary on top of the daemon-backed Elixir runtime |
| 2026-03-31T19:37:37Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/cli_test.exs` | `main@working-tree` | `validated` | the core operator path now supports `--json` for doctor, sessions, load-session, chat, resume-session, cancel-session, and daemon lifecycle commands; `sessions --json` emits compact summaries instead of full transcripts; `docs/reference/tui.md` defines the terminal UI as a thin client over the Elixir daemon/runtime; `TMPDIR=$PWD/.tmp mix test test/claw_code/cli_test.exs` passed with 28 tests green; and `./claw_code doctor --provider generic --no-tools --json`, `./claw_code chat --provider generic --json hello`, `./claw_code daemon status --json`, and `./claw_code sessions --json` all worked against the built escript | land the JSON client boundary, then decide whether the first TUI should shell out to the JSON CLI or speak the daemon protocol directly |
| 2026-03-31T20:02:06Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/qa.sh daemon` | `main@00d8198` | `landed` | committed as `feat(tui): add daemon-backed terminal client`; `./claw_code tui` now auto-connects to or starts the local daemon, shows recent sessions plus selected transcript/receipts, and supports `chat`, `resume`, `open`, `cancel`, `tools`, `refresh`, `help`, and `quit`; `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs test/claw_code/cli_test.exs` passed with 31 tests green, `TMPDIR=$PWD/.tmp ./scripts/qa.sh daemon` passed with 34 tests green, and `printf 'quit\n' | ./claw_code tui --provider generic --no-tools --daemon-root $PWD/.tmp/tui-daemon --session-root $PWD/.tmp/tui-sessions` rendered the TUI successfully | decide whether the next TUI slice should add provider/model switching in-client or move first into direct daemon polling and richer pane updates |
| 2026-03-31T20:09:49Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs test/claw_code/cli_test.exs` | `main@working-tree` | `validated` | the TUI now supports `provider <name|default>`, `model <name|default>`, `base-url <url>`, `clear base-url`, and `next` / `prev` session navigation; the header shows the active base URL and selected session position; `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs test/claw_code/cli_test.exs` passed with 34 tests green; and `printf 'provider kimi\nmodel kimi-k2.5\nprovider default\nmodel default\nquit\n' | ./claw_code tui --provider generic --no-tools --daemon-root $PWD/.tmp/tui-daemon-iter3 --session-root $PWD/.tmp/tui-sessions-iter3` proved the in-client inference switching and reset path against the built escript | land the TUI control iteration, then add scalable session browsing like filters, limit changes, or `open latest|running|failed` without bloating the client |
| 2026-03-31T20:15:24Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs test/claw_code/cli_test.exs` | `main@working-tree` | `validated` | the TUI now separates loaded sessions from the visible filtered list, supports `filter <all|running|completed|failed>`, `limit <n>`, and `open latest|running|failed`, and keeps session browsing local to the client instead of pushing UI state into the daemon; `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs test/claw_code/cli_test.exs` passed with 36 tests green; and `printf 'filter failed\nopen failed\nfilter all\nopen running\nlimit 2\nquit\n' | ./claw_code tui --provider generic --no-tools --daemon-root $PWD/.tmp/tui-daemon-scale --session-root $PWD/.tmp/tui-sessions-scale` proved real filtered browsing, status-targeted open, and list scaling against seeded sessions | land the session-scaling TUI slice, then add richer targets such as `open completed` or `find <substring>` if the simple list starts feeling too coarse |
| 2026-03-31T20:34:37Z | `missions/02-provider-loop.md` | `#11`, `#13` | `providers`, `core`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/provider_test.exs test/claw_code/runtime_test.exs test/claw_code/cli_test.exs test/claw_code/tui_test.exs` | `main@working-tree` | `validated` | added `./claw_code probe` plus TUI `probe`, preserved full query-string chat-completions URLs, normalized legacy `function_call` plus map-shaped tool arguments, and added one-shot fallback without `tools` / `tool_choice` when an auto-tool generic endpoint rejects tool parameters; the focused test bundle passed with 61 tests green | run the full required repo gates, then land and push the provider-compatibility slice so tonight’s operator path is `probe -> daemon/tui -> chat` |
| 2026-03-31T20:39:10Z | `missions/02-provider-loop.md` | `#11`, `#13` | `providers`, `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | full repo validation passed with 97 tests green after adding `CLAW_API_KEY_HEADER` / `--api-key-header`, generic auth-header override support, persisted `api_key_header` in provider snapshots, and doctor output for the header source; `./claw_code probe --provider generic --json` returned the clean missing-config payload and `./claw_code doctor --provider generic --api-key test-key --api-key-header api-key` rendered the explicit header override | land and push the provider-compatibility slice, then use `probe -> daemon/tui -> chat` against the real Kimi or custom GLM endpoint |
| 2026-03-31T22:05:16Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs test/claw_code/cli_test.exs test/claw_code/session_store_test.exs` | `main@working-tree` | `validated` | added shared session substring search in `SessionStore.list/1`, `sessions --query`, TUI `find <substring>` and `clear find`, plus `open completed`; the TUI header now surfaces the active query and the search applies across ids, prompts, outputs, provider names, and message content | run the focused tests and a real TUI smoke, then land the session-search slice for daily-driver navigation |
| 2026-03-31T22:07:28Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | full repo validation passed with 100 tests green after moving TUI `find` to a client-side visible-session lens, keeping `all_sessions` intact for aliases like `open completed`; `./claw_code sessions --limit 10 --query beta --session-root $PWD/.tmp/tui-sessions-find` returned only `session-running`, and `printf 'find beta\nclear find\nopen completed\nquit\n' | ./claw_code tui --no-tools --daemon-root $PWD/.tmp/tui-daemon-find --session-root $PWD/.tmp/tui-sessions-find` proved substring search, search reset, and completed-session open in the built escript | land the session-search slice and keep tightening the daily-driver TUI around session navigation instead of adding streaming complexity |
| 2026-03-31T22:32:48Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | added session-ref aliases across `load-session`, `resume-session`, and `cancel-session`, plus TUI `open latest-completed` and targeted `resume latest-completed <prompt>` semantics; full repo validation passed with 104 tests green, and `./claw_code load-session latest-completed --session-root $PWD/.tmp/alias-smoke` resolved the correct completed session in the built escript | land the alias-navigation slice with a semantic commit, then keep tightening session ergonomics for daily-driver use |
| 2026-03-31T22:49:28Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | added transcript-local `find-msg`, `clear find-msg`, `next-hit`, and `prev-hit` state in the TUI without changing the daemon contract; full repo validation passed with 106 tests green, and `printf 'find-msg beta\nnext-hit\nprev-hit\nquit\n' | ./claw_code tui --provider generic --no-tools --daemon-root $PWD/.tmp/tui-transcript-daemon --session-root $PWD/.tmp/tui-transcript-smoke` proved transcript hit navigation against a seeded completed session in the built escript | land the transcript-navigation slice, then keep tightening daily-driver visibility around active session state instead of adding transport complexity |
| 2026-03-31T22:54:54Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | added aggregate run counts, selected-session run metadata, and last-receipt summaries to the TUI without changing daemon semantics; full repo validation passed with 107 tests green, and `printf 'open running\nquit\n' | ./claw_code tui --provider generic --no-tools --daemon-root $PWD/.tmp/tui-status-daemon --session-root $PWD/.tmp/tui-status-smoke` rendered mixed running/completed/failed counts plus running-session receipt details in the built escript | land the active-visibility slice, then keep tightening daily-driver monitoring around refresh cadence and selected-session shortcuts |
| 2026-03-31T23:10:19Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | added TUI `watch <seconds|on|off>` auto-refresh cadence plus a defensive `SessionStore.list/1` path that ignores malformed or non-object JSON files under the session root; full repo validation passed with 109 tests green, and a delayed-input smoke moved the built TUI from `runs=running:0 completed:1 failed:0` to `runs=running:1 completed:1 failed:0` after a background session update while watch mode stayed at `watch=1s` | land the watch-mode slice, then keep tightening active-run monitoring and selected-session shortcuts without changing the daemon contract |
| 2026-03-31T23:22:10Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | added TUI `follow <latest|running|latest-running|completed|failed|off>` so watch mode can auto-select a target session on each refresh without changing daemon semantics; full repo validation passed with 111 tests green, and a delayed-input smoke moved the built TUI from `follow=running` on a completed session to `selected_run=running since=2026-03-31T23:25:01Z` with `id=session-running` after a background session appeared | land the follow-target slice, then keep tightening daily-driver monitoring around active-session commands rather than widening the transport boundary |
| 2026-03-31T23:28:39Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | added an `active` alias plus `focus active` / `focus all` TUI presets so monitoring live work becomes one command that sets filter, follow target, and watch cadence together; full repo validation passed with 113 tests green, and `printf 'focus active\nquit\n' | ./claw_code tui --provider generic --no-tools --daemon-root $PWD/.tmp/tui-focus-active-daemon --session-root $PWD/.tmp/tui-focus-active-smoke` rendered `sessions=1/2 filter=running`, `watch=1s follow=running`, and kept `id=session-running` selected in the built client | land the active-focus slice, then keep tightening daily-driver intervention commands around targeted cancel/resume on active sessions |
| 2026-03-31T23:39:16Z | `docs/reference/tui.md` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | added alias-aware TUI cancellation so `cancel active`, `cancel running`, and `cancel selected` share the same session-resolution path as `open` and `resume`; full repo validation passed with 115 tests green, and `printf 'cancel active\nquit\n' | ./claw_code tui --provider generic --no-tools --daemon-root $PWD/.tmp/tui-cancel-active-daemon --session-root $PWD/.tmp/tui-cancel-active-smoke` moved the built client from `selected_run=running since=...` to `selected_run=idle last_stop=cancelled` with `notice=Cancelled smoke-running.` and persisted `session_stop=cancelled` | land the targeted-cancel slice, then keep tightening daily-driver intervention around targeted resume and active-session shortcuts |
| 2026-03-31T23:52:10Z | `openspec/changes/reach-daily-driver` | `#6`, `#7`, `#8`, `#9`, `#10`, `#11`, `#12`, `#13` | `architect`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `in_progress` | created the OpenSpec planning layer with project context plus proposal, design, tasks, and a daily-driver spec delta; linked the roadmap from README, `.omx/README.md`, `.omx/board.md`, `docs/execution-plan.md`, and `docs/backlog.md` so the remaining path to daily-driver status is now canonical instead of spread across chat and ad hoc docs | run the full repo validation gate, then land the OpenSpec roadmap slice with a semantic docs commit |
| 2026-03-31T23:55:24Z | `openspec/changes/reach-daily-driver` | `#6`, `#7`, `#8`, `#9`, `#10`, `#11`, `#12`, `#13` | `architect`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` passed with 115 tests green and a rebuilt escript after adding `openspec/project.md`, the daily-driver change set, and the new roadmap links in `README.md`, `.omx/README.md`, `.omx/board.md`, `docs/execution-plan.md`, and `docs/backlog.md` | land the OpenSpec roadmap slice as one semantic docs commit and push it to `origin/main` |
| 2026-04-01T00:10:16Z | `openspec/changes/reach-daily-driver` | `#6`, `#13` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/session_server_test.exs test/claw_code/cli_test.exs test/claw_code/tui_test.exs` | `main@working-tree` | `validated` | added a monotonic terminal-state guard so late `finish_run` calls no longer overwrite cancelled session state, added richer CLI/TUI session diagnostics for provider/model and prompt/output summaries, and wrote `docs/reference/original-workflow-parity.md`; focused tests passed with 60 tests green | run the full repo validation gate, then land the Phase 1 parity-and-durability slice with a semantic commit and push |
| 2026-04-01T00:11:11Z | `openspec/changes/reach-daily-driver` | `#6`, `#13` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | full repo validation passed with 116 tests green plus a rebuilt `claw_code` escript after adding the original-workflow parity note, stronger session diagnostics, and the cancelled-run terminal-state guard | land and push the Phase 1 parity-and-durability slice as one semantic commit |
| 2026-04-01T00:22:57Z | `openspec/changes/reach-daily-driver` | `#6`, `#13` | `core`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/qa.sh daemon`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | added daemon restart continuity coverage, explicit `stop_timeout` failure handling, recovery of persisted `run=running` sessions into `run_interrupted`, and a stale-daemon recovery smoke in `scripts/ralph-daemon.sh`; the daemon Ralph lane now logs `status: stale -> running -> stopping`, and full repo validation passed with 119 tests green | land and push the daemon recovery slice, then move to the next Phase 2 provider portability gap |
| 2026-04-01T00:35:50Z | `openspec/changes/reach-daily-driver` | `#11` | `providers`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/qa.sh provider`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `main@working-tree` | `validated` | added provider capability summaries plus generic minimal-payload fallback in `OpenAICompatible`, updated `doctor` / `probe` output and the provider Ralph loop, and captured live GLM + NIM evidence through `doctor -> probe -> chat`; full repo validation passed with 122 tests green | land and push the provider portability slice, then keep driving the remaining provider matrix and generic-endpoint evidence |
| 2026-04-01T16:46:35Z | repo-operating-system | `#10` | `architect` | docs setup | `main@working-tree` | `validated` | added top-level `VISION.md` and `DESIGN.md` so the repo now has one short product-intent document and one short system-shape document, then linked both from `README.md` for one-hop discovery | keep using the new top-level docs as the canonical entrypoint while the remaining daily-driver slices land under OpenSpec and OMX |
| 2026-04-01T16:48:45Z | repo-operating-system | `#10` | `architect` | `./claw_code summary` | `main@working-tree` | `validated` | added top-level `TECHSTACK.md` as the concrete runtime inventory, widened the README entrypoint to `VISION.md`, `DESIGN.md`, and `TECHSTACK.md`, and confirmed the built escript still reports the expected runtime surface through `./claw_code summary` | keep the top-level docs aligned with the real runtime as provider, adapter, and TUI work continues |
| 2026-04-01T16:58:56Z | `openspec/changes/reach-daily-driver` | `#6`, `#12`, `#13` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `./claw_code load-session broken-smoke --session-root $PWD/.tmp/invalid-session-smoke` | `main@working-tree` | `validated` | tightened session-root hygiene by centralizing session decode validation, returning typed invalid-session errors, and making `load-session`, `resume-session`, `cancel-session`, `chat --session-id`, and session-server startup fail locally against one broken file instead of crashing or silently replacing it; full repo validation passed with 128 tests green, and the built escript now reports `Session state is invalid for broken-smoke ...` against a malformed session file | move to the next daily-driver slice around provider setup and matrix evidence, while keeping the top-level docs aligned with the runtime contract |
| 2026-04-01T17:09:51Z | `openspec/changes/reach-daily-driver` | `#11`, `#10` | `providers`, `architect`, `qa` | `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `./claw_code providers`; `TMPDIR=$PWD/.tmp RALPH_MAX_CYCLES=1 ./scripts/qa.sh provider-matrix` | `main@working-tree` | `validated` | added a first-class `./claw_code providers` matrix view, a new `provider-matrix` Ralph lane, a checked-in `.env.local.example`, and explicit provider setup docs so operators can inspect all supported providers and validate either the live or missing-config path from one surface; full repo validation passed with 132 tests green, `./claw_code probe --provider generic` now renders the missing-config path without crashing, and the provider-matrix loop completed across `generic`, `glm`, `kimi`, and `nim` with mixed live and missing-config evidence | keep driving Phase 2 with live Kimi plus generic-endpoint evidence, then tighten provider visibility in the TUI header/footer |
| 2026-04-01T17:26:08Z | `openspec/changes/reach-daily-driver` | `#6`, `#10`, `#13` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `printf 'help\nopen failed\nquit\n' | ./claw_code tui --provider generic --no-tools --daemon-root $PWD/.tmp/tui-health-smoke-daemon --session-root $PWD/.tmp/tui-health-smoke-sessions` | `main@working-tree` | `validated` | tightened the TUI operator surface by simplifying `provider_health` to a compact status plus one explicit `missing=` field list, centralizing the footer and `help` command inventory behind one command-summary helper so alias coverage cannot drift, and adding a compact operator quickstart to `docs/reference/tui.md`; focused TUI tests passed with 24 tests green, full repo validation passed with 134 tests green, and the rebuilt client now renders `selected_health=failed ...` alongside `latest-running`, `latest-completed`, and `latest-failed` aliases in both the footer and `help` notice | keep driving Phase 4 with either tighter active-session inspect shortcuts or better large-session-root navigation, while preserving the thin-client CLI/daemon boundary |
| 2026-04-01T17:29:04Z | `openspec/changes/reach-daily-driver` | `#6`, `#10`, `#13` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `printf 'help\ninspect failed\nquit\n' | ./claw_code tui --provider generic --no-tools --daemon-root $PWD/.tmp/tui-health-smoke-daemon --session-root $PWD/.tmp/tui-health-smoke-sessions` | `main@working-tree` | `validated` | added an explicit `inspect` TUI shortcut over the same alias resolver already used by `open`, so resume, inspect, and cancel now share the same active/latest/failed target pattern without widening the daemon boundary; focused TUI tests passed with 25 tests green, full repo validation passed with 135 tests green, the rebuilt client now renders `inspect [selected|n|id|latest|active|running|latest-running|completed|latest-completed|failed|latest-failed]` in both the footer and `help` notice, and the `inspect failed` smoke reopened the failed session from the live client | keep driving Phase 4 with better large-session-root navigation, or move into Phase 5 daemon-health detail while keeping the TUI a thin client over CLI and daemon state |
| 2026-04-01T17:32:09Z | `openspec/changes/reach-daily-driver` | `#6`, `#10`, `#13` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `printf 'resume selected continue from the last state\nquit\n' | ./claw_code tui --provider generic --no-tools --daemon-root $PWD/.tmp/tui-resume-selected-smoke-daemon --session-root $PWD/.tmp/tui-resume-selected-smoke-sessions` | `main@working-tree` | `validated` | completed the active-session shortcut pass by teaching TUI `resume selected ...` to reuse the current selection over the same alias-driven target surface as `inspect` and `cancel`, and backed it with a daemon-backed TUI test plus updated operator docs/spec text; focused TUI tests passed with 27 tests green, full repo validation passed with 137 tests green, and the live client reused the selected failed generic session instead of forcing the operator back to a raw session id | move to the next Phase 4 slice around larger session roots, while keeping the TUI on the thin-client CLI and daemon boundary |
| 2026-04-01T17:40:30Z | `openspec/changes/reach-daily-driver` | `#6`, `#10`, `#13` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs --seed 0`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `printf 'find alpha\nquit\n' | ./claw_code tui --provider generic --no-tools --limit 1 --daemon-root $PWD/.tmp/tui-find-root-smoke-daemon --session-root $PWD/.tmp/tui-find-root-smoke-sessions` | `main@working-tree` | `validated` | moved the Phase 4 large-session-root slice forward by making TUI `find` search through the session root instead of only the newest limited buffer, while keeping the same thin-client build-state path and existing filter/limit controls; focused TUI tests passed with 30 tests green, full repo validation passed with 140 tests green, and the live smoke started on `session-beta` at `--limit 1` before `find alpha` surfaced and selected the older `session-alpha` from the same root | keep tightening large-session-root usability, likely with explicit paging or older/newer navigation, without moving session state or provider logic into the client |
| 2026-04-01T17:42:07Z | `openspec/changes/reach-daily-driver` | `#6`, `#10`, `#13` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `printf 'open session-05\nquit\n' | ./claw_code tui --provider generic --no-tools --limit 12 --daemon-root $PWD/.tmp/tui-window-smoke-daemon --session-root $PWD/.tmp/tui-window-smoke-sessions` | `main@working-tree` | `validated` | finished the remaining Phase 4 large-session-root view slice by windowing the rendered session list around the current selection and showing transcript tail excerpts with absolute message numbering, so the client no longer dumps the full loaded list or loses message position context as histories grow; focused TUI tests passed with 30 tests green, full repo validation passed with 140 tests green, and the live smoke rendered `showing 4-11/12 sessions` plus `showing 3-8/8 messages` after opening `session-05` from a seeded 12-session root | move into explicit older/newer paging or Phase 5 daemon-health detail while keeping the TUI on the thin-client CLI and daemon boundary |
| 2026-04-01T17:55:32Z | `openspec/changes/reach-daily-driver` | `#6`, `#10`, `#13` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/session_store_test.exs test/claw_code/tui_test.exs`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `printf 'older\nnewer\nquit\n' | ./claw_code tui --provider generic --no-tools --limit 2 --daemon-root $PWD/.tmp/tui-page-smoke-daemon --session-root $PWD/.tmp/tui-page-smoke-sessions` | `feat/tui-paging@working-tree` | `validated` | extended the Phase 4 large-session-root path with explicit TUI `older` / `newer` paging over `SessionStore.list(offset: ...)`, added page-position metadata in the header, and kept the behavior inside the existing thin-client state rebuild path instead of introducing a new daemon surface; focused store plus TUI tests passed with 41 tests green, full repo validation passed with 142 tests green, and the live smoke moved from `page=1-2/5` with `selected=1/5` to `page=3-4/5` with `selected=3/5`, then back to the newest page | decide whether to keep tightening Phase 4 with direct `latest`/jump paging, or switch to Phase 5 daemon-health and recovery detail while this feature branch remains the current landing lane |
| 2026-04-01T18:14:59Z | `openspec/changes/reach-daily-driver` | `#6`, `#10`, `#13` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/session_store_test.exs test/claw_code/daemon_test.exs test/claw_code/cli_test.exs`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `./claw_code daemon start --daemon-root $PWD/.tmp/daemon-health-smoke-daemon --session-root $PWD/.tmp/daemon-health-smoke-sessions`; `./claw_code daemon status --daemon-root $PWD/.tmp/daemon-health-smoke-daemon`; `./claw_code daemon stop --daemon-root $PWD/.tmp/daemon-health-smoke-daemon` | `feat/daemon-health-status@working-tree` | `validated` | added a canonical session-root health summary in `SessionStore.health/1` and surfaced it through `daemon status`, so operators now get busy/failed/partially recovered signals, aggregate counts, and `latest_running` / `latest_failed` / `latest_recovered` summaries with recent receipt detail on one command; focused store, daemon, and CLI tests passed with 60 tests green, full repo validation passed with 146 tests green, and the built escript smoke rendered `health: busy, failed, partially_recovered` plus the expected latest-session triage lines | keep moving through Phase 5 by proving repeated background session continuity and codifying the common recovery playbooks, while the next architecture slice for real vision use remains a provider-agnostic multimodal input boundary rather than a model-name-only change |
| 2026-04-01T18:46:26Z | `openspec/changes/reach-daily-driver` | `#11`, `#6`, `#10` | `providers`, `core`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/multimodal_test.exs test/claw_code/provider_test.exs test/claw_code/runtime_test.exs test/claw_code/cli_test.exs test/claw_code/session_store_test.exs`; `mix format --check-formatted`; `TMPDIR=$PWD/.tmp mix test`; `TMPDIR=$PWD/.tmp mix escript.build`; `./claw_code summary`; `./claw_code symphony --native "review MCP tool"`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `./claw_code chat --provider generic --base-url http://127.0.0.1:1/v1 --model smoke-model --session-root $PWD/.tmp/multimodal-smoke.ypVzJz --session-id multimodal-smoke --image $PWD/demo.gif "describe this image"`; `./claw_code load-session multimodal-smoke --session-root $PWD/.tmp/multimodal-smoke.ypVzJz --show-messages` | `feat/multimodal-input@working-tree` | `validated` | completed the Phase 2 multimodal provider-boundary slice by standardizing replayable persisted `input_image` parts in `ClawCode.Multimodal`, validating local image paths before provider I/O, translating them to OpenAI-style `image_url` parts only at the provider edge, preserving repeated `--image` flags across the daemon boundary, and rendering/searching mixed message content cleanly in CLI and session queries; focused multimodal tests passed with 90 tests green, full repo validation passed with 157 tests green, and the built escript smoke rendered `2. user: describe this image [image:demo.gif]` after an explicit provider-edge failure | keep driving Phase 2 with live multimodal evidence on a real preferred provider, then return to Phase 5 recovery and repeated-background-session continuity |
| 2026-04-01T19:02:31Z | `openspec/changes/reach-daily-driver` | `#13`, `#11`, `#10` | `core`, `providers`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs test/claw_code/cli_test.exs test/claw_code/provider_test.exs test/claw_code/multimodal_test.exs`; `mix format --check-formatted`; `TMPDIR=$PWD/.tmp mix test`; `TMPDIR=$PWD/.tmp mix escript.build`; `./claw_code summary`; `./claw_code symphony --native "review MCP tool"`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `printf 'chat --image \"$PWD/demo.gif\" describe this image\nquit\n' | ./claw_code tui --provider generic --base-url http://127.0.0.1:1/v1 --model smoke-model --daemon-root $PWD/.tmp/tui-image-smoke-daemon --session-root $PWD/.tmp/tui-image-smoke-sessions` | `feat/tui-multimodal-commands@working-tree` | `validated` | pushed the multimodal path into the thin-client daily-driver surface: the TUI now advertises `input_modalities=text, image`, accepts `chat [--image PATH]...` plus `resume ... [--image PATH]...`, forwards repeated image attachments through the existing daemon/runtime/provider path, and renders selected-session multimodal messages as compact `[image:...]` summaries instead of raw maps; focused TUI plus provider/CLI/multimodal tests passed with 96 tests green, full repo validation passed with 165 tests green, and the rebuilt client smoke rendered both the multimodal capability line and `2. user: describe this image [image:demo.gif]` after an explicit provider-edge failure | use a real preferred vision-capable provider for live TUI evidence next, then decide whether the next OpenSpec slice is Phase 2 live multimodal proof or Phase 5 repeated-background-session continuity |
| 2026-04-01T19:02:33Z | `openspec/changes/reach-daily-driver` | `#10`, `#11`, `#13` | `core`, `providers`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/tui_test.exs test/claw_code/cli_test.exs test/claw_code/provider_test.exs`; `mix format --check-formatted`; `TMPDIR=$PWD/.tmp mix test`; `TMPDIR=$PWD/.tmp mix escript.build`; `./claw_code summary`; `./claw_code symphony --native "review MCP tool"`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `printf 'chat --image $PWD/demo.gif describe this image\nquit\n' | ./claw_code tui --provider generic --base-url http://127.0.0.1:1/v1 --model smoke-model --no-tools --daemon-root $PWD/.tmp/tui-image-smoke.saeIPw/daemon --session-root $PWD/.tmp/tui-image-smoke.saeIPw/sessions` | `feat/tui-multimodal-input@working-tree` | `validated` | extended the thin-client operator loop so TUI `chat` and `resume` now forward repeated `--image PATH` inputs through the existing daemon/runtime boundary, surfaced provider `input_modalities` in `doctor`, `providers`, `probe`, and the TUI header, and kept selected-transcript rendering compact with `[image:...]` markers instead of raw content-part maps; focused provider, CLI, and TUI tests passed with 94 tests green, full repo validation passed with 165 tests green, and the built escript smoke rendered `input_modalities=text, image` plus `2. user: describe this image [image:demo.gif]` after a TUI-issued multimodal request hit an explicit provider-edge failure | keep pushing the vision daily-driver path with live provider evidence for a real image-capable model, then decide whether the next local slice should be TUI image-path ergonomics or Phase 5 background-session continuity |
| 2026-04-01T19:14:05Z | `openspec/changes/reach-daily-driver` | `#10`, `#11`, `#13` | `architect`, `providers` | `./claw_code summary` | `feat/roadmap-competitive-plan@working-tree` | `validated` | added top-level `ROADMAP.md` as the short readiness and execution-order view, linked it from README, `VISION.md`, `.omx/board.md`, and `docs/execution-plan.md`, and used official Gemini CLI, OpenCode, and Google Antigravity sources to turn the competitive feature study into a concrete adoption plan; as of 2026-04-01 the roadmap keeps the main-tool bar explicit, prioritizes Phase 5 continuity plus live provider evidence first, and notes that no public official Antigravity repo was located during the study | execute the P0 roadmap slices next: daemon continuity hardening, live provider and multimodal proof on the preferred GLM path, then shell or adapter reliability work |
| 2026-04-01T19:24:33Z | `openspec/changes/reach-daily-driver` | `#13`, `#10` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/daemon_test.exs test/claw_code/cli_test.exs`; `mix format --check-formatted`; `TMPDIR=$PWD/.tmp mix escript.build`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `./claw_code daemon start --daemon-root $PWD/.tmp/daemon-root-mismatch-smoke/daemon --session-root $PWD/.tmp/daemon-root-mismatch-smoke/sessions`; `./claw_code chat --daemon --daemon-root $PWD/.tmp/daemon-root-mismatch-smoke/daemon --session-root $PWD/.tmp/daemon-root-mismatch-smoke/conflict --provider generic --base-url http://127.0.0.1:1/v1 --api-key test-key --model test-model hello`; `./claw_code daemon stop --daemon-root $PWD/.tmp/daemon-root-mismatch-smoke/daemon` | `feat/roadmap-competitive-plan@working-tree` | `validated` | closed the clearest remaining Phase 5 silent-continuity hole by making the daemon-owned session root authoritative for daemon-backed `chat` and `cancel-session`, rejecting conflicting client roots with an explicit operator-facing error instead of silently forking state across multiple session directories; added focused daemon plus CLI regression coverage, updated the daily-driver spec and README, full repo validation passed with 169 tests green, and the built escript smoke now returns `Daemon session root mismatch: daemon owns ... but request used ...` with exit code 1 | keep pushing tonight's P0 cut line with daemon startup recovery of abandoned `run=running` sessions next, then shift to live GLM multimodal proof and the remaining recovery playbooks |
| 2026-04-01T19:31:28Z | `openspec/changes/reach-daily-driver` | `#13`, `#10` | `core`, `architect`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/session_store_test.exs test/claw_code/session_server_test.exs test/claw_code/daemon_test.exs test/claw_code/cli_test.exs`; `mix format --check-formatted`; `TMPDIR=$PWD/.tmp mix escript.build`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh` | `feat/roadmap-competitive-plan@working-tree` | `validated` | added a shared `SessionStore` recovery helper for persisted `run_state.status=running` sessions, reused it from `SessionServer` startup, and made `Daemon.serve` sweep the session root before becoming ready so abandoned runs are rewritten to explicit `run_interrupted` state before health reporting; focused store, session-server, daemon, and CLI tests passed with 76 tests green, full repo validation passed with 169 tests green, daemon and CLI health expectations now reflect recovered abandoned sessions instead of falsely reporting them as live work, and the roadmap/README/spec now reflect that this continuity gap is closed | keep pushing tonight's P0 cut line with live GLM multimodal proof next, then land the remaining Phase 5 repair playbooks and crash-consistency hardening |
| 2026-04-01T19:41:34Z | `openspec/changes/reach-daily-driver` | `#11`, `#10`, `#13` | `providers`, `architect`, `qa` | `./claw_code probe --provider glm`; `./claw_code probe --provider glm --image $PWD/demo.gif`; `./claw_code probe --provider glm --model GLM-4.5V --image $PWD/.tmp/glm-real-image.jpg`; `./claw_code probe --provider glm --model GLM-4.6V --image $PWD/.tmp/glm-real-image.jpg`; `./claw_code probe --provider glm --model GLM-5 --image $PWD/.tmp/glm-real-image.jpg`; `./claw_code chat --provider glm --model GLM-4.6V --no-tools --session-root $PWD/.tmp/glm-vision-chat/sessions --session-id glm-vision-smoke --image $PWD/.tmp/glm-real-image.jpg "Briefly describe this image in one sentence."`; `printf 'chat --image $PWD/.tmp/glm-real-image.jpg Briefly describe this image in one sentence.\nquit\n' | ./claw_code tui --provider glm --model GLM-4.6V --no-tools --daemon-root $PWD/.tmp/glm-vision-chat/daemon --session-root $PWD/.tmp/glm-vision-chat/sessions` | `feat/roadmap-competitive-plan@working-tree` | `validated` | captured real GLM multimodal evidence on the preferred provider lane: the configured coding endpoint answered `probe` successfully on text with `GLM-4.7`, rejected image input on `GLM-4.7`, accepted live multimodal `probe` on both `GLM-4.5V` and `GLM-4.6V`, returned a 429 plan-access error for `GLM-5` in this environment, and completed both direct `chat` and thin-client `tui` multimodal image-description flows on `GLM-4.6V`; updated the provider docs and roadmap to record `GLM-4.6V` as the currently proven vision-capable GLM path and noted that no official public `GLM-5V-Turbo` coding-plan page was located as of 2026-04-01 | keep pushing tonight's cut line by documenting the remaining recovery playbooks, then close the remaining live-evidence gap for a generic endpoint and, if credentials appear, Kimi |
| 2026-04-01T20:09:05Z | `openspec/changes/reach-daily-driver` | `#11`, `#13`, `#10` | `providers`, `core`, `qa` | `TMPDIR=$PWD/.tmp mix format --check-formatted`; `TMPDIR=$PWD/.tmp mix test test/claw_code/multimodal_test.exs test/claw_code/runtime_test.exs test/claw_code/cli_test.exs`; `TMPDIR=$PWD/.tmp mix test`; `TMPDIR=$PWD/.tmp mix escript.build`; `./claw_code summary`; `./claw_code symphony --native "review MCP tool"`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `./claw_code providers`; `./claw_code probe --provider glm --model GLM-5.1 "Reply with OK."`; `./claw_code chat --provider glm --model GLM-5.1 --vision-provider glm --vision-model GLM-4.6V --no-tools --session-root $PWD/.tmp/glm-split-vision-smoke --session-id glm-split-vision-smoke --image $PWD/.tmp/glm-real-image-1775072450-18499.jpg "Briefly describe this image and explain why a split vision backbone was used."`; `./claw_code load-session glm-split-vision-smoke --session-root $PWD/.tmp/glm-split-vision-smoke --show-messages`; `printf 'chat --image %s Briefly describe this image in one sentence.\nquit\n' "$PWD/.tmp/glm-real-image-1775072450-18499.jpg" | ./claw_code tui --provider glm --model GLM-5.1 --vision-provider glm --vision-model GLM-4.6V --no-tools --daemon-root $PWD/.tmp/glm-split-tui-smoke-daemon --session-root $PWD/.tmp/glm-split-tui-smoke-sessions` | `feat/roadmap-competitive-plan@working-tree` | `validated` | added an optional split vision-backbone path across CLI, daemon, runtime, and session rendering so image understanding can come from one provider or model while the primary reasoning request stays text-only; `Multimodal` now persists compact replayable `vision_context` parts, the runtime fails explicitly on incomplete split vision config, daemon-backed flows preserve the new `--vision-*` flags, and operator surfaces now render `Vision backbone: ...` plus `vision=...` instead of hiding the extra context; focused tests passed with 76 tests green, full repo validation passed with 178 tests green, `./claw_code providers` confirmed `kimi` still lacks credentials in this environment, `./claw_code probe --provider glm --model GLM-5.1` succeeded live, and the split path `GLM-5.1` reasoning plus `GLM-4.6V` vision completed successfully through both `chat` and the TUI with persisted `[vision:glm/GLM-4.6V]` transcript context | finish the remaining Phase 5 repair playbooks and capture the last live provider-evidence gaps for one generic endpoint and Kimi once credentials are available |
| 2026-04-01T20:19:07Z | `openspec/changes/reach-daily-driver` | `#12`, `#13`, `#10` | `core`, `qa`, `architect` | `TMPDIR=$PWD/.tmp mix format --check-formatted`; `TMPDIR=$PWD/.tmp mix test test/claw_code/tools_test.exs test/claw_code/cli_test.exs test/claw_code/runtime_test.exs test/claw_code/tui_test.exs`; `TMPDIR=$PWD/.tmp mix test`; `TMPDIR=$PWD/.tmp mix escript.build`; `./claw_code summary`; `./claw_code symphony --native "review MCP tool"`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `mix run -e 'alias ClawCode.Tools.Builtin; IO.inspect(Builtin.execute_with_receipt("shell", %{"command" => "rm -rf /tmp/example"}, allow_shell: true))'` | `feat/roadmap-competitive-plan@working-tree` | `validated` | finished the Phase 3 shell or write safety slice by persisting a per-run permissions snapshot, surfacing explicit `shell_access` and `write_access` in `doctor`, echoing permissions in chat and session inspection output, attaching policy metadata to blocked or allowed shell and write receipts, and rendering blocked destructive shell policy details like `blocked_shell_prefix:rm` instead of opaque blocked-tool output; focused tool, CLI, runtime, and TUI tests passed with 115 tests green, full repo validation passed with 182 tests green, and the smoke command returned a blocked shell receipt with `rule=blocked_shell_prefix` and `blocked_prefix=rm` | push the validated checkpoint to the remote branch, then continue with the remaining Phase 5 repair playbooks and the last live provider-evidence gaps |
| 2026-04-01T20:25:52Z | `openspec/changes/reach-daily-driver` | `#12`, `#10`, `#13` | `adapters`, `core`, `qa` | `TMPDIR=$PWD/.tmp mix test test/claw_code/host_test.exs test/claw_code/runtime_test.exs`; `mix run -e 'alias ClawCode.Host; IO.inspect(Host.run_runtime_with_receipt(:python, "import sys; sys.stderr.write(\"python-boom\\\\n\"); sys.stderr.flush(); raise SystemExit(7)"))'` | `feat/roadmap-competitive-plan@working-tree` | `validated` | closed the remaining Phase 3 adapter-proof slice by threading optional `timeout_ms` through `python_eval`, `lua_eval`, and `lisp_eval`, annotating runtime receipts with `runtime`, `engine`, and `invocation`, and proving direct host-adapter failure semantics for Python, Lua, and Common Lisp with explicit nonzero exit status, merged stderr/stdout output, and timeout receipts; added a runtime regression showing a provider-triggered `python_eval` timeout survives into persisted session receipts, updated the OpenSpec task/spec/design plus the roadmap and README, and the focused host plus runtime tests passed with 31 tests green | push the validated branch checkpoint, then take the next remaining daily-driver blockers: repair playbooks, one meaningful Common Lisp-backed tool path, and the remaining live provider evidence gaps |
| 2026-04-01T20:43:13Z | `openspec/changes/reach-daily-driver` | `#8`, `#10`, `#12` | `adapters`, `qa`, `architect` | `TMPDIR=$PWD/.tmp mix format --check-formatted`; `TMPDIR=$PWD/.tmp mix test`; `TMPDIR=$PWD/.tmp mix escript.build`; `TMPDIR=$PWD/.tmp ./scripts/qa.sh adapters`; `./claw_code summary`; `./claw_code symphony --native "review MCP tool"`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `mix run -e 'alias ClawCode.Tools.Builtin; IO.puts(elem(Builtin.execute_with_receipt("sexp_outline", %{"source" => "(defpackage :demo) (defun hello (name) (format t \\\"Hello, ~A\\\" name))"}), 1))'` | `feat/roadmap-competitive-plan@working-tree` | `validated` | closed the remaining Phase 3 Common Lisp-adapter justification slice by wiring env forwarding through `Host`, quieting SBCL startup noise, and proving the existing `sexp_outline` tool as a structured Common Lisp-backed analysis path beyond raw eval; added focused coverage for direct `sexp_outline` execution, provider-triggered persistence, and Common Lisp env propagation, updated the adapter Ralph loop, README, and OpenSpec task/spec/design, and the slice now renders clean outlines like `forms=2 shown=2 max_depth=2` with top-level `defpackage` and `defun` summaries instead of forcing arbitrary `lisp_eval` | take the next remaining blockers in order: daemon recovery playbooks, then native disable or fallback evidence plus the Elixir-first versus Zig rule |
| 2026-04-01T21:19:04Z | `openspec/changes/reach-daily-driver` | `#13`, `#10`, `#12` | `core`, `qa`, `architect` | `TMPDIR=$PWD/.tmp mix format --check-formatted`; `TMPDIR=$PWD/.tmp ./scripts/qa.sh recovery`; `TMPDIR=$PWD/.tmp mix test`; `TMPDIR=$PWD/.tmp mix escript.build`; `./claw_code summary`; `./claw_code symphony --native "review MCP tool"`; `TMPDIR=$PWD/.tmp ./scripts/validate-repo.sh`; `bash -n scripts/*.sh` | `feat/roadmap-competitive-plan@working-tree` | `validated` | closed the remaining Phase 5 recovery-playbook gap by adding `docs/reference/recovery.md` as the canonical operator doc for stale-daemon replacement, abandoned-running-session reconciliation, corrupted-session handling, and daemon session-root mismatch, plus `scripts/ralph-recovery.sh` and `./scripts/qa.sh recovery` as the corresponding evidence lane; updated README, the execution plan, the OMX board, and the OpenSpec task/spec/design so recovery now lives as an explicit operator surface instead of scattered notes, and the new recovery lane passed end to end with stale metadata, `run_interrupted` recovery, invalid-session failure, and daemon-root mismatch all rendered explicitly; full repo validation passed with 193 tests green | take the next remaining daily-driver blockers in order: one generic and Kimi live-provider proof if credentials exist, then native disable or fallback evidence plus the explicit Elixir-versus-Zig extension rule |
